---
import 'animate.css';

---
    <!-- <section class="scroll-container">
        <img src="pics/colada.png" alt="Picture of logo 1" class="scroll-image from-left">
    </section>

    <section class="scroll-container1">
    <img src="pics/archive.png" alt="Picture of logo 2" class="scroll-image from-right">
    </section> -->

    <section class="animate__animated animate__slideInLeft styleimg1">
        <div class="img-container animate__animated animate__slideInLeft styleimg1">
            <img src="pics/colada.png" alt="Picture of logo 1" class="img1">
        </div>
    </section>
    <section class="animate__animated animate__slideInRight styleimg2">
        <div class="img-container animate__animated animate__slideInRight styleimg2">
            <img src="pics/archive.png" alt="Picture of logo 2" class="img2">
        </div>
    </section>


    <style>

    @media (max-width: 799px) {
    .styleimg1 {
      display: none;
    }
    .styleimg2{
        display: none;
    }
    }
    @media (min-width: 800px) {
        :root {
    --animate-duration: 2s;
    }

    .styleimg1{
        margin-top: 8rem;
        margin-left: 5%;
    }

    .styleimg2{
        margin-top: 0.5rem;
        margin-left: 16%;
    }

    .img1{
        width:100%;
        height: auto; 
    }

    .img2{
        width:104%;
        height: auto; 
    }
    }


    .img-container {
        width: 53rem;
        height: auto;
    }

    
    </style>
 

<!-- <style>



.scroll-container, .scroll-container1 {
    display: none;
}
@media (min-width: 800px){

    .scroll-container, .scroll-container1 {
    display: block;
}

.scroll-container, .scroll-container1 {
    position: relative;
    width: 100%;
    height: 100px;
    overflow: hidden;
}

.scroll-container {
    margin-top: 50px;
}
.scroll-container1 {
    margin-bottom: 50px;
}

.scroll-image {
    position: absolute;
    width: 50%;
    height: 100%;
    transition: transform 1s ease; 
    transform: translateX(-100%); 
}

.from-left {
    left: -200px;
    z-index: 2;
}

.from-right {
    right: -200px;
    z-index: 1;
}
}
</style>

<script>
let lastScrollTop = 0;

window.addEventListener("scroll", function() {
    var scrollPosition = window.scrollY;
    var scrollCenter = window.innerHeight / 2;

    var scrollImages = document.querySelectorAll('.scroll-image');

    for (let i = 0; i < scrollImages.length; i++) {
        var image = scrollImages[i];
        var imagePosition = (image.getBoundingClientRect() as DOMRect).top;
        var imageCenter = imagePosition + image.offsetHeight / 2;

        var translateValue = (scrollPosition - imageCenter) / 4; 

        if (Math.abs(translateValue) < window.innerWidth / 2) {
            if (scrollPosition > lastScrollTop) {

                if (image.classList.contains('from-left')) {
                    (image as HTMLElement).style.transform = "translateX(" + translateValue + "px)";
                } else {
                    (image as HTMLElement).style.transform = "translateX(-" + Math.abs(translateValue) + "px)";
                }
            } else {
                if (image.classList.contains('from-left')) {
                    (image as HTMLElement).style.transform = "translateX(-" + Math.abs(translateValue) + "px)";
                } else {
                    (image as HTMLElement).style.transform = "translateX(" + translateValue + "px)";
                }
            }
        } else {
            (image as HTMLElement).style.transform = "translateX(-100%)";
        }
    }

    lastScrollTop = scrollPosition;
}); -->
</script>